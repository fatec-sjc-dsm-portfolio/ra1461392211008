// const ring = document.getElementById('ring');

// document.addEventListener('mousemove', (e) => {
//   const ringSize = ring.offsetWidth / 2;
//   ring.style.left = `${e.clientX - ringSize}px`;
//   ring.style.top = `${e.clientY - ringSize}px`;
// });
